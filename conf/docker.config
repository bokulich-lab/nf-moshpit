docker.enabled              = true
docker.runOptions           = "-v ${params.containerCacheDir}:/cache"
process.container           = ""
if (params.tempDir) {
    docker.temp             = params.tempDir
}
if (params.executor) {
    process.executor        = params.executor
}
